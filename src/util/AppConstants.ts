const appConstants = {
    backendHost: process.env.BACKEND_HOST || "https://staging.order-system.octoberserver.net",
    mainAppURL: process.env.MAIN_APP_URL || "https://zl-order-system.github.io/staging/app/",
}
export default appConstants;

